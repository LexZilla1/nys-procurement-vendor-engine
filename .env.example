# Copy this file to .env and fill in your real key. .env is gitignored and must
# NEVER be committed. Do not put real secrets in THIS file (.env.example is
# committed as a template with placeholders only).

# Anthropic API key for this project (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
