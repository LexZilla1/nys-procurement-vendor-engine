"""engine — daily-habit backend state primitives (PR 1).

Pure, in-memory domain objects: Citation, DatedObligation + ObligationGraph,
the tender-lifecycle state machine, and outcome/observation capture. No UI, no
network, no payment-clock logic, no numeric confidence/risk scores.
"""
